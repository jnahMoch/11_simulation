services:
  - type: web
    name: seven-eleven-simulation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web/server.py $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
